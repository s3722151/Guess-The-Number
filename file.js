public class GuessNumber {
    public static void main(String[] args) {
      // Step 1: Generate a random number between 1 and 5
      // Use Java's Random class or Math.random() method
      // (Hint: You need to import java.util.Random if you use the Random class)
  
      // Step 2: Prompt the user to guess the number
      // (Hint: You will need to take input from the user; consider using a Scanner)
  
      // Step 3: Read the user's input and store it in a variable
      // (Hint: Use Scanner's nextInt() method to read an integer)
  
      // Step 4: Compare the user's guess with the randomly generated number
      // (Hint: Use an if-else statement to check if the guess is correct)
  
      // Step 5: If the guess is incorrect, print a message telling the user they are wrong
      // (Example: "Number 4 is incorrect")
  
      // Step 6: If the guess is correct, print a message telling the user they are right
      // (Example: "The number is 3")
      
      // Step 7: End the program after displaying the result
    }
  }
  